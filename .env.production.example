# SyncKairos Production Environment Variables
# Copy to .env.production and fill in values
# NEVER commit .env.production to git!

# Application
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# Security
# Generate with: openssl rand -base64 32
JWT_SECRET=<generate-with-openssl-rand-base64-32>

# Redis (Primary State Store) - Set in Component 4.3
# Format: rediss://:<password>@<host>:<port>
REDIS_URL=rediss://your-redis-connection-string

# PostgreSQL (Audit Store) - Set in Component 4.3
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://your-postgres-connection-string

# Optional: Monitoring (Set in Component 4.3)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# GRAFANA_REMOTE_WRITE_URL=https://your-grafana-instance/api/prom/push
